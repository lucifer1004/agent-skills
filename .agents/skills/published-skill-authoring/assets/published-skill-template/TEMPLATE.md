# Published Skill Template

Copy this template when starting a new published skill repository.

Replace every `your-skill` placeholder with the real bundled skill name.

Keep the bundled skill under `skills/your-skill/` and treat that subtree as the runtime-facing core.
